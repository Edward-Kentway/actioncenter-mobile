/**
 * Base application definition for the Push Notification App.
 */

var pushNotificationApp = angular.module('pushNotification', []);

// Set up all application controllers
pushNotificationApp.controller('PushNotificationFormController', function($scope) {

});
